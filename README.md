This repo contains code to reproduce an issue with react-snap.

To demonstrate the issue, run:

```shell
yarn && yarn build
```

The react-snap process should crash with `Cannot write to stream after nil`.
